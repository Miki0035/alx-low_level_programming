#include "main.h"

/**
 * more_numbers - prints 10 times the number
 */

void more_numbers(void)
{
	for (int i = 0; i < 10; i++)
	{
		for (int j = 0; j <= 14; j++)
		{
			if (j = 14)
			{
				_putchar(j);
			}
			else
			{
				_putchar(j);
			}
		}
		_putchar('\n');
	}
}
