#include "main.h"
/**
 * _puts_recursion - prints string
 * @s : parameter for string value
 * Returns - void
 */

void _puts_recursion(char *s)
{
	int i;

	i = 0;
	while (s[i] != '\0')
	{
		_putchar(s[i];
		i++;
	}
}
